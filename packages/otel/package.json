{
  "name": "@movibe/logger-otel",
  "version": "2.0.0",
  "description": "OpenTelemetry bridge for @movibe/logger — emit OTel spans + logs",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": { "types": "./dist/index.d.ts", "default": "./dist/index.js" },
      "require": { "types": "./dist/index.d.ts", "default": "./dist/index.cjs" }
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir=dist --target=node --format=esm --external=@opentelemetry/api --external=@movibe/logger && bun build ./src/index.ts --target=node --format=cjs --outfile=dist/index.cjs --external=@opentelemetry/api --external=@movibe/logger",
    "build:types": "npx tsc --emitDeclarationOnly",
    "build:all": "bun run build && bun run build:types",
    "test": "vitest run",
    "prepack": "bun run build:all"
  },
  "peerDependencies": {
    "@movibe/logger": "^2.0.0",
    "@opentelemetry/api": "^1.7.0",
    "@opentelemetry/api-logs": "^0.50.0"
  },
  "devDependencies": {
    "@movibe/logger": "workspace:*",
    "@opentelemetry/api": "^1.9.0",
    "@opentelemetry/api-logs": "^0.50.0",
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/movibe/logger.git"
  },
  "author": "Movibe",
  "license": "MIT"
}
